# Publie nginx sur le port 80 de l'hôte (déploiement sans Traefik qui écoute déjà sur :80).
# Usage :
#   docker compose -f docker-compose.prod-no-letsencrypt.yml -f docker-compose.prod-no-letsencrypt.publish-80.yml up -d

services:
  nginx:
    ports:
      - "80:80"

import type { Metadata } from "next";
import { DEFAULT_APP_DISPLAY_NAME } from "@/lib/branding";

export const landingMetadata: Metadata = {
  title: `${DEFAULT_APP_DISPLAY_NAME} — IA conversationnelle souveraine`,
  description:
    "MonaChat : l’IA au service de vos équipes, développée par CIACEMS — beta ouverte, stack maîtrisée, confiance et budget accessibles.",
};
